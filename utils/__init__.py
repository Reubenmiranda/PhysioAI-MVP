"""
Utility package for PhysioAI.

This file marks `utils` as a Python package so that modules such as
`pose_module`, `exercise_logic`, and `session_manager` can be imported
cleanly from different entrypoints (e.g. Flask app, scripts).
"""

